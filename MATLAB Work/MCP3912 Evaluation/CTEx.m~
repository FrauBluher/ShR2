close all;
%% MCP3912 Initial Testing
% Linear Sweep 0-8kHz
Y = fft(VarName3, 2^16);
Pyy = Y.*conj(Y) / 4096;
f = 35200/4096*(0:2047);

loglog(f, mag2db(Pyy(1:2048)))

title('PSD: 1-kHz Square Wave Excitation of CT');
xlabel('Frequency (Hz)');
